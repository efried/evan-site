-- Do not manually edit this file, it was auto-generated by dillonkearns/elm-graphql
-- https://github.com/dillonkearns/elm-graphql


module Github.Enum.RepositoryInteractionLimit exposing (..)

import Json.Decode as Decode exposing (Decoder)


{-| A repository interaction limit.

  - ExistingUsers - Users that have recently created their account will be unable to interact with the repository.
  - ContributorsOnly - Users that have not previously committed to a repository’s default branch will be unable to interact with the repository.
  - CollaboratorsOnly - Users that are not collaborators will not be able to interact with the repository.
  - NoLimit - No interaction limits are enabled.

-}
type RepositoryInteractionLimit
    = ExistingUsers
    | ContributorsOnly
    | CollaboratorsOnly
    | NoLimit


list : List RepositoryInteractionLimit
list =
    [ ExistingUsers, ContributorsOnly, CollaboratorsOnly, NoLimit ]


decoder : Decoder RepositoryInteractionLimit
decoder =
    Decode.string
        |> Decode.andThen
            (\string ->
                case string of
                    "EXISTING_USERS" ->
                        Decode.succeed ExistingUsers

                    "CONTRIBUTORS_ONLY" ->
                        Decode.succeed ContributorsOnly

                    "COLLABORATORS_ONLY" ->
                        Decode.succeed CollaboratorsOnly

                    "NO_LIMIT" ->
                        Decode.succeed NoLimit

                    _ ->
                        Decode.fail ("Invalid RepositoryInteractionLimit type, " ++ string ++ " try re-running the @dillonkearns/elm-graphql CLI ")
            )


{-| Convert from the union type representing the Enum to a string that the GraphQL server will recognize.
-}
toString : RepositoryInteractionLimit -> String
toString enum____ =
    case enum____ of
        ExistingUsers ->
            "EXISTING_USERS"

        ContributorsOnly ->
            "CONTRIBUTORS_ONLY"

        CollaboratorsOnly ->
            "COLLABORATORS_ONLY"

        NoLimit ->
            "NO_LIMIT"


{-| Convert from a String representation to an elm representation enum.
This is the inverse of the Enum `toString` function. So you can call `toString` and then convert back `fromString` safely.

    Swapi.Enum.Episode.NewHope
        |> Swapi.Enum.Episode.toString
        |> Swapi.Enum.Episode.fromString
        == Just NewHope

This can be useful for generating Strings to use for <select> menus to check which item was selected.

-}
fromString : String -> Maybe RepositoryInteractionLimit
fromString enumString____ =
    case enumString____ of
        "EXISTING_USERS" ->
            Just ExistingUsers

        "CONTRIBUTORS_ONLY" ->
            Just ContributorsOnly

        "COLLABORATORS_ONLY" ->
            Just CollaboratorsOnly

        "NO_LIMIT" ->
            Just NoLimit

        _ ->
            Nothing
